
目录及文件说明

bsp_cli        客户端命令行相关，用于测试bsp_api.so

bsp_client     客户接口调用实现，实现了gsoap_api.h的客户端部分

bsp_server.c   server端的main入口及硬件抽象层主进程。
bsp_server.h

common         server端公共类代码

czmq           第三方工具，用于进程间消息通信，目前使用到的功能不多。

gsoap          gsoap的支持

gsoap_api.c    gsoap server端接口实现文件

hw             硬件接口层实现文件

Makefile       你懂的

release        所有的目标文件输出目录，包括bsp_server程序，对外接口(libbsp_api.so, libbsp_api.a, bsp_api.h) bsp_cli程序


